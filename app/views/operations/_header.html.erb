<h2>Operation - Opération</h2>

<%= form_with(url: search_operations_path, method: :get, local: true) do %>
  <div>
    <div>
      <%= text_field_tag(:keywords, nil, placeholder: "Search for Operations - Rechercher des opérations") %>
    </div>
    <div>
      <%= submit_tag("Search - Chercher") %>
    </div>
  </div>
<% end %>